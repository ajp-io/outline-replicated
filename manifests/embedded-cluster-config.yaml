apiVersion: embeddedcluster.replicated.com/v1beta1
kind: Config
metadata:
  name: outline-enterprise
spec:
  version: "3.0.0-alpha-26+k8s-1.33"   # verify latest: replicated embedded-cluster versions
